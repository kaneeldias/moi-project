export type Project = {
    id: string,
    name: string,
    location: string,
    sdg: number,
    startDate: Date,
    endDate: Date
}

export type Opportunity = {
    id: number,
    name: string,
    location: string,
    sdg: number,
}